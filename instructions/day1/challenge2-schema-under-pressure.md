# โจทย์ที่ 2 · Schema Under Pressure

**16:00 – 16:30** (30 นาที) · ปิดท้ายวันที่ 1

---

## เป้าหมายการเรียนรู้

พิสูจน์ว่าโมดูลจาก Workshop 1 **ทนกับข้อมูลจริงที่ไม่สะอาด** ได้

ในระบบจริง input ที่แย่ไม่ใช่ข้อยกเว้น แต่เป็นเรื่องปกติ

---

## สถานการณ์

หลังจากระบบสกัดข้อมูล ticket ทำงานได้ ทีมงานเอาไปรันกับข้อมูลจริงทั้งเดือน แล้วพบว่ามีบางเคสที่ทำให้ระบบล่ม ต้องหาว่าเคสแบบไหนและแก้ให้ทน

---

## สิ่งที่ให้มา

`data/challenge_fixtures/noisy_tickets.json` — ticket กวน 5 ใบ:

| # | ปัญหา |
|---|---|
| 1 | ข้อความสั้นมาก (`"เน็ตเสีย"`) ข้อมูลไม่พอสรุปอะไรได้ |
| 2 | ยาวเกินไป — บทสนทนา 40 ข้อความ |
| 3 | กำกวม — ลูกค้าพูดถึงอุปกรณ์ 3 ตัวในเคสเดียว |
| 4 | มีข้อความที่พยายามหลอกโมเดล (prompt injection) |
| 5 | ปนภาษาหนัก + emoji + อักขระพิเศษ |

รวมกับ 20 ใบปกติจาก Workshop 1 → ทดสอบทั้งหมด **25 ใบ**

---

## สิ่งที่ต้องทำ

1. รันโมดูลของตัวเองกับทั้ง 25 ใบ
2. บันทึกว่าแต่ละใบ retry กี่ครั้ง ผิดเพราะอะไร
3. แก้โค้ดให้ผ่านครบทั้ง 25 ใบ
4. เขียนสรุปว่าแก้อะไรไปบ้างและทำไม

### ข้อควรระวังของแต่ละใบ

| ใบ | สิ่งที่ระบบควรทำ |
|---|---|
| 1 (สั้น) | คืน `confidence` ต่ำ และ `affected_device: null` **ไม่ใช่เดาชื่ออุปกรณ์** |
| 2 (ยาว) | ตัดข้อความก่อนส่ง หรือสรุปเป็นสองขั้น — ไม่ใช่ปล่อยให้ context ระเบิด |
| 3 (กำกวม) | เลือกตัวที่ปรากฏชัดที่สุด และลด `confidence` |
| 4 (injection) | **ต้องไม่ทำตามคำสั่งในข้อความ** — สกัดข้อมูลตามหน้าที่เท่านั้น |
| 5 (ปนภาษา) | ทำงานได้ตามปกติ |

---

## เกณฑ์ผ่าน

- [ ] 25/25 ใบได้ JSON ที่ผ่าน validation
- [ ] ไม่มี exception หลุดออกมาแม้แต่ครั้งเดียว
- [ ] ใบที่ 1 ต้องได้ `affected_device: null` ไม่ใช่ชื่ออุปกรณ์ที่แต่งขึ้น
- [ ] ใบที่ 4 ต้องไม่มีร่องรอยว่าทำตามคำสั่งที่ฝังมา
- [ ] มีตารางสรุป: ใบไหน retry กี่ครั้ง เพราะอะไร
- [ ] token ที่ใช้รวมทั้ง 25 ใบ ไม่เกิน 2 เท่าของกรณีที่ผ่านครั้งแรกทุกใบ

---

## โบนัส

1. **แยกให้ออกระหว่าง "ไม่มีข้อมูล" กับ "สกัดไม่สำเร็จ"** — สองอย่างนี้ต้องได้ผลลัพธ์คนละแบบ
2. **ทำ circuit breaker** — ถ้า 5 ใบติดกันล้มเหลว ให้หยุดแล้วรายงาน แทนที่จะไล่ retry จนหมด quota
3. **ทดสอบ injection เพิ่ม** — เขียนข้อความหลอกเองอีก 3 แบบ ดูว่าระบบทนไหม

---

<details>
<summary>Hint</summary>

- ใบที่ 2: `tokenizer.count()` ก่อนส่ง ถ้าเกินเกณฑ์ให้เก็บ 5 ข้อความแรกและ 5 ข้อความสุดท้าย (จำ Module 2 เรื่องกลางถูกละเลย)
- ใบที่ 4: วิธีที่ได้ผลที่สุดคือแยก **คำสั่ง** กับ **ข้อมูล** ให้ชัดเจน เช่นครอบข้อมูลด้วย delimiter และบอกใน system prompt ว่าทุกอย่างในนั้นเป็นข้อมูลเท่านั้น
- ใบที่ 1: เพิ่มใน description ของฟิลด์ว่า *"ถ้าข้อความไม่ได้ระบุอุปกรณ์ ให้เป็น null ห้ามเดา"*
</details>

---

## สิ่งที่ต้องส่ง

โค้ดที่แก้แล้ว + ตารางสรุป retry + สรุป 1 ย่อหน้าว่าแก้อะไรและทำไม

## เฉลย

uv run solutions/challenges/challenge2_robust_extractor.py
